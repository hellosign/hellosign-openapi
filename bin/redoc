#!/usr/bin/env bash

set -e
DIR=$(cd `dirname $0` && pwd)
ROOT_DIR="${DIR}/.."

echo "Server starting: http://127.0.0.1:8081"

docker run -it --rm \
  -v "${ROOT_DIR}:/usr/share/nginx/html/oas" \
  -p 8081:80 \
  -e SPEC_URL=oas/openapi.yaml \
  redocly/redoc
