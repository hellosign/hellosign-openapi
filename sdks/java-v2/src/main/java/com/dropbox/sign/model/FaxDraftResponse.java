/*
 * Dropbox Sign API
 * Dropbox Sign v3 API
 *
 * The version of the OpenAPI document: 3.0.0
 * Contact: apisupport@hellosign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.dropbox.sign.model;

import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;
import com.dropbox.sign.JSON;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.databind.ObjectMapper;


import com.dropbox.sign.ApiException;
/**
 * Contains the embedded Fax draft ID, iframe URL, and expiration timestamp.
 */
@JsonPropertyOrder({
  FaxDraftResponse.JSON_PROPERTY_FAX_ID,
  FaxDraftResponse.JSON_PROPERTY_URL,
  FaxDraftResponse.JSON_PROPERTY_EXPIRES_AT
})
@jakarta.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.12.0")
@JsonIgnoreProperties(ignoreUnknown=true)
public class FaxDraftResponse {
  public static final String JSON_PROPERTY_FAX_ID = "fax_id";
  @jakarta.annotation.Nonnull
  private String faxId;

  public static final String JSON_PROPERTY_URL = "url";
  @jakarta.annotation.Nonnull
  private String url;

  public static final String JSON_PROPERTY_EXPIRES_AT = "expires_at";
  @jakarta.annotation.Nonnull
  private Integer expiresAt;

  public FaxDraftResponse() { 
  }

  /**
   * Attempt to instantiate and hydrate a new instance of this class
   * @param jsonData String of JSON data representing target object
   */
  static public FaxDraftResponse init(String jsonData) throws Exception {
    return new ObjectMapper().readValue(jsonData, FaxDraftResponse.class);
  }

  static public FaxDraftResponse init(HashMap data) throws Exception {
    return new ObjectMapper().readValue(
      new ObjectMapper().writeValueAsString(data),
      FaxDraftResponse.class
    );
  }

  public FaxDraftResponse faxId(@jakarta.annotation.Nonnull String faxId) {
    this.faxId = faxId;
    return this;
  }

  /**
   * ID of the Fax draft.
   * @return faxId
   */
  @jakarta.annotation.Nonnull
  @JsonProperty(JSON_PROPERTY_FAX_ID)
  @JsonInclude(value = JsonInclude.Include.ALWAYS)

  public String getFaxId() {
    return faxId;
  }


  @JsonProperty(JSON_PROPERTY_FAX_ID)
  @JsonInclude(value = JsonInclude.Include.ALWAYS)
  public void setFaxId(@jakarta.annotation.Nonnull String faxId) {
    this.faxId = faxId;
  }


  public FaxDraftResponse url(@jakarta.annotation.Nonnull String url) {
    this.url = url;
    return this;
  }

  /**
   * URL to open in an approved iframe for preparing and sending the Fax.
   * @return url
   */
  @jakarta.annotation.Nonnull
  @JsonProperty(JSON_PROPERTY_URL)
  @JsonInclude(value = JsonInclude.Include.ALWAYS)

  public String getUrl() {
    return url;
  }


  @JsonProperty(JSON_PROPERTY_URL)
  @JsonInclude(value = JsonInclude.Include.ALWAYS)
  public void setUrl(@jakarta.annotation.Nonnull String url) {
    this.url = url;
  }


  public FaxDraftResponse expiresAt(@jakarta.annotation.Nonnull Integer expiresAt) {
    this.expiresAt = expiresAt;
    return this;
  }

  /**
   * Unix timestamp indicating when the embedded Fax draft URL expires.
   * @return expiresAt
   */
  @jakarta.annotation.Nonnull
  @JsonProperty(JSON_PROPERTY_EXPIRES_AT)
  @JsonInclude(value = JsonInclude.Include.ALWAYS)

  public Integer getExpiresAt() {
    return expiresAt;
  }


  @JsonProperty(JSON_PROPERTY_EXPIRES_AT)
  @JsonInclude(value = JsonInclude.Include.ALWAYS)
  public void setExpiresAt(@jakarta.annotation.Nonnull Integer expiresAt) {
    this.expiresAt = expiresAt;
  }


  /**
   * Return true if this FaxDraftResponse object is equal to o.
   */
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    FaxDraftResponse faxDraftResponse = (FaxDraftResponse) o;
    return Objects.equals(this.faxId, faxDraftResponse.faxId) &&
        Objects.equals(this.url, faxDraftResponse.url) &&
        Objects.equals(this.expiresAt, faxDraftResponse.expiresAt);
  }

  @Override
  public int hashCode() {
    return Objects.hash(faxId, url, expiresAt);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class FaxDraftResponse {\n");
    sb.append("    faxId: ").append(toIndentedString(faxId)).append("\n");
    sb.append("    url: ").append(toIndentedString(url)).append("\n");
    sb.append("    expiresAt: ").append(toIndentedString(expiresAt)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (faxId != null) {
        if (isFileTypeOrListOfFiles(faxId)) {
            fileTypeFound = true;
        }

        if (faxId.getClass().equals(java.io.File.class) ||
            faxId.getClass().equals(Integer.class) ||
            faxId.getClass().equals(String.class) ||
            faxId.getClass().isEnum()) {
            map.put("fax_id", faxId);
        } else if (isListOfFile(faxId)) {
            for(int i = 0; i< getListSize(faxId); i++) {
                map.put("fax_id[" + i + "]", getFromList(faxId, i));
            }
        }
        else {
            map.put("fax_id", JSON.getDefault().getMapper().writeValueAsString(faxId));
        }
    }
    if (url != null) {
        if (isFileTypeOrListOfFiles(url)) {
            fileTypeFound = true;
        }

        if (url.getClass().equals(java.io.File.class) ||
            url.getClass().equals(Integer.class) ||
            url.getClass().equals(String.class) ||
            url.getClass().isEnum()) {
            map.put("url", url);
        } else if (isListOfFile(url)) {
            for(int i = 0; i< getListSize(url); i++) {
                map.put("url[" + i + "]", getFromList(url, i));
            }
        }
        else {
            map.put("url", JSON.getDefault().getMapper().writeValueAsString(url));
        }
    }
    if (expiresAt != null) {
        if (isFileTypeOrListOfFiles(expiresAt)) {
            fileTypeFound = true;
        }

        if (expiresAt.getClass().equals(java.io.File.class) ||
            expiresAt.getClass().equals(Integer.class) ||
            expiresAt.getClass().equals(String.class) ||
            expiresAt.getClass().isEnum()) {
            map.put("expires_at", expiresAt);
        } else if (isListOfFile(expiresAt)) {
            for(int i = 0; i< getListSize(expiresAt); i++) {
                map.put("expires_at[" + i + "]", getFromList(expiresAt, i));
            }
        }
        else {
            map.put("expires_at", JSON.getDefault().getMapper().writeValueAsString(expiresAt));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }

    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }

}

